[project]
name = "narrative-brief"
version = "0.1.0"
description = "Narrated visual briefings for phone review"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "pillow>=10"]

[tool.pytest.ini_options]
testpaths = ["tests"]
